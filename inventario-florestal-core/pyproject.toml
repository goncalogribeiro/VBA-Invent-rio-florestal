[project]
name = "inventario-florestal-core"
version = "0.1.0"
description = "Sistema técnico-científico para inventário florestal, biometria e prognose"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
